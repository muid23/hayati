[project]
name = "hayati"
version = "0.4.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "alMukaafih", email = "almukaafih@gmail.com" }]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "google-genai>=2.22.0",
    "python-dotenv>=1.2.3",
    "websockets>=16.1.1",
]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.fastapi]
entrypoint = "hayati:app"
